# Claude Code — Project Instructions

This repo's agent-facing documentation is tool-agnostic and lives in
[`AGENTS.md`](AGENTS.md). Read that first — it's the entry point for any AI
agent working in this repo, including you.

Don't duplicate its content here. If you need Claude-Code-specific notes in
the future, add them below this line; keep everything else in `AGENTS.md` and
`docs/` so both this file and other tools' agent files stay pointed at one
source of truth.
