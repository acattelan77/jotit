# 2026-07-13 — Version 1.3.1 publish

Agent/session: Codex (GPT-5)

## What I did

Bumped Jot it! from `1.3.0` to `1.3.1` in `package.json`, `manifest.json`,
and the visible side-panel footer. This patch release includes the new-note
title auto-fill fix and the export-state flush fix from the preceding
handoffs.

## Why

The product owner asked to push the fixes and bump the version number before
continuing toward Chrome Web Store release readiness.

## Verified

Verification was run before commit:

- `npm test`
- `node --check sidepanel.js`
- `node --check panel/state.mjs`
- `jq empty manifest.json`
- `git diff --check`

## Docs updated

Added this handoff entry. The behavior-specific specs were updated in the
preceding fixes:

- `docs/specs/context-title-suggestion.md`
- `docs/specs/export-and-save.md`

## Left for next time

Reload the unpacked extension in Chrome and manually confirm the side-panel
footer shows `v. 1.3.1`, New note captures the current page title, and Save As
does not leave the extension with stale note content after export.
